[tool.poetry]
name = "autostep"
version = "0.1.0"
description = "A numpyro implementation of autoStep methods"
authors = ["Miguel Biron-Lattes <miguelbl9@gmail.com>"]
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/miguelbiron/autostep"

[tool.poetry.dependencies]
python = "^3.12"
numpyro = "^0.16.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
